import { QuantityMask } from './quantity-mask';

describe('QuantityMask', () => {
  it('should create an instance', () => {
    const directive = new QuantityMask();
    expect(directive).toBeTruthy();
  });
});
